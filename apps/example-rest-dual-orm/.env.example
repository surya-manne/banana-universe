# MongoDB — used by src/modules/tags (Mongoose)
MONGODB_URI=mongodb://127.0.0.1:27017/banana_dual_orm

# PostgreSQL — used by src/modules/widgets (TypeORM) in non-test runs
DATABASE_URL=postgres://postgres:postgres@localhost:5432/widgets

# HTTP
PORT=3000
